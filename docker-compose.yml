version: '3.7'

services:
    redis:
      image: redis:alpine
      container_name: redis
    bookmark-service:
      image: bookmark-service-test:latest
      build: bookmark-service
      container_name: bookmark-service
      depends_on:
        - redis
      env_file:
        - .env